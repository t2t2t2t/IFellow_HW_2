package Cars;

public class BMW extends Car{
    public BMW(String model, int year, String color, int horsepower, boolean isAutomatic, double price) {
        super("BMW", model, year, color, horsepower, isAutomatic, price);
    }
}
